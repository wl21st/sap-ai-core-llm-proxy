# Spec: No Behavior Changes

This change removes dead code only. There are no new or modified capabilities.

All existing API contracts remain unchanged:
- `/v1/chat/completions` — unchanged
- `/v1/messages` — unchanged
- `/v1/embeddings` — unchanged
- `/v1/models` — unchanged

No streaming behavior changes for any live code path.
